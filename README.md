# use this command to run
* `npx json-server db.js -r routes.json`

# API mock file tree structure

mock
├── parameters.json
├── profile
│   ├── item.json
│   └── shoppint-cart.json
├── system
│   └── checkIp.json
└── user
    └── me.json